# {{PROJECT_NAME}}

Project-based programming learning repository for **{{TOPIC_NAME}}**.

## Current Focus

- Level: {{LEVEL}}
- Roadmap: {{ROADMAP}}
- Conda environment: `{{ENV_NAME}}`
- Learning records: {{OBSIDIAN_TOPIC_PATH}}

## Setup

```powershell
conda create -n {{ENV_NAME}} python=3.11
conda activate {{ENV_NAME}}
pip install -U pip
```

Install project dependencies as each stage introduces them.

## Learning Workflow

This repository uses the `project-bloom` skill. Work in small stages, verify each stage before moving forward, and update the Obsidian learning records after meaningful progress.

## Git

Remote: {{REPO_URL}}

Use SSH remotes by default. Do not push until you intentionally create a checkpoint.
